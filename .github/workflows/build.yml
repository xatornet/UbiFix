name: Compilar Plugin ASI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: # Permite ejecutarlo manualmente desde la pestaña Actions

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Clonar el repositorio
      # Actualizado a v4 para evitar errores de obsolescencia
      uses: actions/checkout@v4 

    - name: Configurar MSVC (Microsoft Visual C++)
      uses: ilammy/msvc-dev-cmd@v1

    - name: Configurar CMake
      run: cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

    - name: Compilar el proyecto
      run: cmake --build build --config Release

    - name: Subir el archivo .asi como artefacto
      # Actualizado a v4 para solucionar el error que mencionas
      uses: actions/upload-artifact@v4 
      with:
        name: PathFix-Plugin
        path: build/Release/PathFix.asi
